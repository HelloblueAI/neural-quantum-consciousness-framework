name: 🧪 Simple Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    name: 🧪 Simple Test Job
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🧪 Simple test
      run: |
        echo "🧠 NeuralCore AGI GitHub Actions Test"
        echo "Repository: ${{ github.repository }}"
        echo "Branch: ${{ github.ref }}"
        echo "Commit: ${{ github.sha }}"
        echo "✅ GitHub Actions are working!"
        
    - name: 📊 Test AGI endpoints
      run: |
        echo "🧪 Testing live AGI endpoints..."
        curl -s http://agi.bleujs.org:8080/health || echo "Health endpoint not accessible"
        curl -s http://agi.bleujs.org:8080/consciousness || echo "Consciousness endpoint not accessible"
        echo "✅ AGI endpoint test completed" 